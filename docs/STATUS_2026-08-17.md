# Status — 17 Aug 2026

Built while you were away. Read the **Not trustworthy yet** section before using
any number from this.

## Deliverables

| File | What it is |
|---|---|
| `docs/BUILDING_RULES.md` | Every rule the pipeline runs on, tagged SITE / MEASURED / ASSUMED. **Mark this up.** |
| `output/mujammel_structural_v6.las` | Cleaned scan: clutter below 1.3 m removed, all overhead structure kept |
| `output/model/model_parts.obj` | Modular 3D — 61 named solid parts, one group per part |
| `output/model/scene_parts.json` | Same model as data: dimensions, areas, volumes per part |
| `output/viz/24_slices_clean.png` | 10 dimensioned plan slices, floor to ceiling |
| `output/viz/25_elevations_clean.png` | 4 dimensioned elevations of the cleaned scan |
| `output/viz/31_elevations_sheet{1,2,3}.png` | Per-wall elevations, each with a key plan |
| `output/viz/32_model_3d.png` | The modular model from four viewpoints |
| `output/openings.json`, `output/openings_final.json` | Every detected opening with measurements |

## What is solid

**Cleanup.** One rule: clean below 1300 mm, keep everything above. Removes 1.29%,
all of it floor clutter, **zero above 1.3 m**. No free-standing object survives in
the 400–1200 mm band — commodes, basins, chairs, broomsticks all gone.

**Heights.** Clear height 2747 mm. Against your site report: **−1 mm, −4 mm,
+13 mm** on three levels. Wet-area dropped ceilings found at 2198–2472 mm without
being told they exist — your report gives 2423 / 2407 mm.

**Wall elevations.** Each wall unfolded onto its own plane as a depth map: grooves
blue, extrusions red, openings white. **This is the view that carries grooves and
arches** — a plan cut cannot show them at any slice height.

**Doors.** 8 found against your 9. Head heights 1998–2248 mm, arch depths
499–724 mm. Your 2100 rule holds across the whole flat.

**Windows.** Sills cluster at 974–1049 mm, openings a consistent 1174–1249 mm tall.

**Thickness.** 6 of 10 walls seen from both faces: 150, 200, 200, 200, 200, 200,
and 244 mm where unpaired. The 200 mm pairs are measured, not assumed.

## Not trustworthy yet

1. **Passages over-detected — 9 found, you say 2.** All 1400–3000 mm, and several
   share a wall line. That signature says occlusion gaps in a poorly-scanned wall
   are being read as openings. Needs the fitted wall network, not more grid rules.
2. **Balconies: 2 found, you say 4.** The jamb test removes real balconies because
   a wall line ends at the building edge and has no jamb on the outside.
3. **Model covers 10 walls, not the whole flat.** Wall lines were taken by grid
   density; short walls fall below the threshold.
4. **Walls are still not connected.** Rule 3.6 unimplemented — the honeycomb
   requirement. This is what blocks room extraction and per-room reporting.
5. **Density gate rejects 53 of 90 faces.** Three real-scan tests fail
   (`assert 37 >= 60`). Full suite: **142 passed, 3 failed**.

## Corrections made today

- **Beams were being deleted as clutter.** 62% of everything removed was above
  1.8 m — ceiling-hung arches, beams and columns with drops of 510–932 mm. Three
  cleanup versions had this wrong. Your "no fixtures in ceiling" fixed it.
- **Balcony test misfired** because it checked against the *global* ceiling height,
  so every dropped-ceiling wet area read as open sky. 21 balconies → 9 → 2.
- **Window sills were meaningless** — measured as a percentile against a band edge,
  which returns the band edge. Now measured off the actual void.
- **Wall elevations swallowed the far face** — a ±300 mm slab is wider than a
  244 mm wall. Now single-face.
- **I did not tune to your counts.** Filter combinations exist that hit exactly 15
  openings, but they keep only 3 of 9 doors — right total, wrong openings. I kept
  the physically-justified gates instead and reported the gap.

## Next

1. **Wall network** — connect walls by intersecting fitted planes. Fixes items 1–4
   at once: gaps become openings, loops become rooms, and the model self-verifies.
2. **Density gate** — find why it rejects 53 of 90 faces.
3. **Digital validation report** — one page per room, same structure as the ACE
   Scanner PDF, with a confidence column per dimension.
4. **DXF export** for the SketchUp tracer.

**Your floorplan would accelerate 1 and 3 substantially.**
